<GlowCheckbox>:
    size: glow_checkbox_icon.size
    size_hint:( None, None)
    GlowIcon:
        id: glow_checkbox_icon
        color: root._color if not root.disabled else self.theme_cls.disabled_color
        adaptive_size: True
        icon: root._icon
        pos: root.pos
